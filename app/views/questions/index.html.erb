<h1>Question</h1>

<ul>
	<% @questions.each do |question| %>
	<li>
		<%= question.title %>
		<%= link_to question.title, question_path(question) %>
	</li>
	<% end %>
</ul>

<p><%= link_to "Add new", new_question_path %></p>

<h1><%= @question.title %></h1>

<p>Yes:<%= @question.n_yes %> No:<%= @question.n_no %></p>

<p><%= @question.created_at %></p>

<p><%= link_to "[Delete]", question_path(@question.id), method: :delete, 
		data: { confirm: "Going to delete, really?" } %></p>
